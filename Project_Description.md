# 拓竹 (Tuozhu) - 3D浮雕手机壳定制平台 项目说明文档
## 1. 项目简介

**拓竹 (Tuozhu)** 是一个集成了 **AIGC (生成式人工智能)** 与 **3D 打印技术** 的手机壳定制平台。用户可以通过简单的图片上传或 AI 文本生成，快速获得高精度的 3D 浮雕模型，并直接导出为可用于 3D 打印的 STL 文件。

本项目解决了传统浮雕建模门槛高、耗时长的痛点，通过深度学习算法自动估算图片深度，并结合 WebGL 技术实现所见即所得的在线定制体验。

---

## 2. 项目结构说明

本项目采用 **前后端分离** 架构，确保了系统的高性能与可扩展性。

### 📂 目录结构概览

```
Tuozhu/
├── src/                    # 前端源代码 (React + Three.js)
│   ├── components/         # UI 组件与 3D 场景组件
│   │   ├── Scene3D.jsx     # 核心 3D 预览场景 (含浮雕渲染与擦除逻辑)
│   │   ├── preview-panel.jsx # 左侧控制面板 (上传、生成、调整)
│   │   └── ...
│   ├── App.jsx             # 主应用逻辑 (STL 导出、状态管理)
│   └── ...
├── backend/                # 深度估计服务
│   └── depth_service/
│       ├── api.py          # 深度图生成 API (基于 PyTorch)
│       └── ...
├── main.py                 # AI 绘图服务 (对接智谱 AI)
├── public/                 # 静态资源 (手机壳模型等)
├── package.json            # 前端依赖配置
└── requirements.txt        # 后端依赖配置
```

### 核心模块解析

1.  **前端 (Frontend)**:
    *   **React**: 构建用户界面。
    *   **Three.js / React Three Fiber**: 实现高性能的网页端 3D 渲染。
    *   **Canvas API**: 用于处理深度图的像素级操作（如擦除功能）。
    *   **STLExporter**: 将生成的几何体转换为标准的 STL 3D 打印格式。

2.  **后端 (Backend)**:
    *   **AI 绘图服务 (`main.py`)**: 集成智谱 AI 接口，通过文本描述生成高质量图像。
    *   **深度估计服务 (`backend/depth_service`)**: 运行深度学习模型，将 2D 图片转换为高精度的灰度深度图，并包含图像增强算法（去背景、细节锐化）。

---

## 3. 使用指南 (玩法介绍)
本仓库链接为https://github.com/3032712445-coder/Tuozhu
若要启动网页，请查看README
本平台操作流程简单直观，仅需五步即可完成定制。

### 第一步：选择手机型号
目前只支持iphone16，之后会继续扩充，选取后可以在右侧预览区看到模型

### 第二步：获取图片
您有两种方式获取想要定制的图案：
*   **上传图片**: 点击“上传图片”按钮，选择本地的照片或设计图。
*   **AI 生成**: 在输入框中描述您想要的画面（例如：“一只赛博朋克风格的猫”），点击“生成图片”，AI 将为您创作独一无二的图案。
### 第二步：生成深度浮雕
*   图片准备好后，点击 **“生成深度图”**。
*   系统会在后台调用深度估计算法，提取画面中的主体轮廓与纹理细节，并自动去除杂乱背景。
*   生成完成后，您将在预览区看到浮雕。

### 第三步：3D 预览与调整 (核心玩法)
此时，右侧的 3D 场景中会实时渲染出浮雕效果。

*   **观察模型**: 按住鼠标左键旋转，滚轮缩放，全方位检查浮雕细节。
*   **参数调整**: 在左侧面板您可以拖动滑块调整浮雕在手机壳上的大小 ，高度，旋转角度。
*   **位置调整**：在右侧可以选择 **“调整位置”** 按钮进入调整模式，此时视角强制回到俯视，鼠标只能拖动浮雕移动，点击调整完毕退出调整
*   **擦除多余部分**:
    1.  在“调整位置”模式下，点击 **“擦除”** 按钮。
    2.  鼠标移动到 3D 模型上。
    3.  按住鼠标左键涂抹您想要去除的浮雕区域（例如多余的背景噪点）。
    4.  松开鼠标，模型会实时更新，被擦除的区域将变平。
    5.  注意：现阶段擦除功能不是很稳定，建议少用。

### 第四步：导出模型
*   确认效果满意后，点击 **“导出 STL”** 按钮。
*   系统将自动合并手机壳模型与您的浮雕设计，生成一个高精度的 `.stl` 文件。
*   您可以将该文件直接导入切片软件（如 Cura, Bambu Studio）进行 3D 打印。

---

## 4. 项目特色与技术亮点

### 🌟 智能深度估计与细节增强
不同于传统的灰度转换，本项目使用了调用了深度学习模型pixel-perfect-depth(https://github.com/gangweix/pixel-perfect-depth)估算单目深度。
*   **Otsu 阈值分割**: 自动识别并剔除背景，确保浮雕主体突出。
*   **高通滤波锐化**: 增强人脸、毛发等微小纹理，让打印出的成品更精致。

### 🎨 实时交互式擦除 (Interactive Eraser)
这是一个极具创新性的功能。用户不仅仅是被动接收 AI 生成的结果，还可以像在 PS 中一样，直接在 3D 模型表面“擦除”不需要的起伏。这通过复杂的 UV 映射与动态纹理技术实现，保证了所见即所得。

### 🚀 高精度网格生成
为了保证打印质量，导出时的网格精度远高于网页预览精度（高达 800x800 网格细分），并包含自动的面剔除算法（Face Culling），自动移除没有任何起伏的平面三角形，极大优化了文件大小和打印效率。

### 🔄 完美的模型融合
导出的 STL 文件不仅仅是浮雕，而是已经与手机壳模型完美贴合的整体。系统自动处理了单位换算（cm 转 mm）、法线计算和几何体合并，确保打印出的手机壳孔位精准，浮雕牢固。

---
